import json

class Quest:
    def __init__(self):
        return
    # def __init__(self, name, number, difficulty, length, questPoints, series, releaseDate, levelRequirements, experienceGranted):
    #     self.name = name
    #     self.number = number
    #     self.difficulty = difficulty
    #     self.length = length
    #     self.questPoints = questPoints
    #     self.series = series
    #     self.releaseDate = releaseDate
    #     self.levelRequirements = levelRequirements # dictionary of skill name to level
    #     self.experienceGranted = experienceGranted # dictionary of skill name to experience